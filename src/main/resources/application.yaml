server:
  port: 8080

spring:
  mail:
    host: smtp-relay.brevo.com
    port: 587
    username: ${BREVO_SMTP_USER:95021f001@smtp-brevo.com}
    password: ${BREVO_SMTP_KEY:rxLODC8YkGEvTQng}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.connectiontimeout: 5000
      mail.smtp.timeout: 5000
      mail.smtp.writetimeout: 5000
  datasource:
    url: jdbc:postgresql://localhost:5442/myturn
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      #ddl-auto: create-drop
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  application:
    name: heg

application:
  security:
    jwt:
      secret-key: "unasecretamuylargaunasecretamuylargaunasecretamuylarga1234"
      expiration: 864000000

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG

activate:
  user:
    mocked: true
openai:
  api:
    key: ${API_KEY}
app:
  mail:
    smtp-from-name: ${SMTP_FROM_NAME}
    smtp-from-email: ${SMTP_FROM_EMAIL}
    is-email-mocked: ${IS_EMAIL_MOCKED}